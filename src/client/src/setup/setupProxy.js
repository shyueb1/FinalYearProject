// const proxy = require('http-proxy-middleware');

// module.exports = function(app) {
//     app.use(proxy('/ws', { target: 'ws://localhost:3001', ws: true }));
//     app.use(proxy('/', { target: 'http://localhost:3001' }));
  
// };

